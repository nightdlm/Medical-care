# 快速测试指南

## 前置条件
1. ✅ MySQL 数据库已启动
2. ✅ Redis 已启动
3. ✅ 已执行 `sql/init.sql` 初始化数据库
4. ✅ 后端 `application.properties` 配置正确（数据库、Redis、AI API）

## 启动步骤

### 1. 启动后端
```bash
cd backend
mvn spring-boot:run
```
等待看到 "Started BackendApplication" 提示

### 2. 启动前端
打开新终端：
```bash
cd frontend
pnpm dev
```
等待看到 "Local: http://localhost:5173/"

## 功能测试

### 测试 1: 基本问诊流程
1. 浏览器访问 http://localhost:5173
2. 在输入框输入：**"我头疼三天了，昨晚开始发烧，体温38.5度"**
3. 点击"提交问诊"按钮
4. 观察：
   - ✅ 显示加载动画
   - ✅ AI 返回结构化结果
   - ✅ 卡片展示主诉、现病史、持续时间等字段
   - ✅ 清空输入框，可以继续新对话

### 测试 2: 风险高亮功能
1. 输入：**"我胸口疼，呼吸困难，出冷汗"**
2. 点击提交
3. 观察：
   - ✅ 结果卡片边框变红
   - ✅ 顶部显示红色警告条："⚠️ 风险提示：检测到高风险症状，建议尽快就医！"
   - ✅ 右上角显示"高风险"徽章
   - ✅ 风险标志字段文字为红色

### 测试 3: 历史记录查询
1. 完成至少 2 次问诊后
2. 点击顶部导航栏"📋 历史记录"
3. 观察：
   - ✅ 显示所有历史问诊记录列表
   - ✅ 每条记录显示时间和症状摘要
   - ✅ 高风险记录有红色标记
   - ✅ 点击记录卡片可展开查看详情
   - ✅ 再次点击收起详情

### 测试 4: 多租户隔离（Token 鉴权）
1. 前端硬编码的 Token: `Bearer test-token-tenant-001`
2. 后端拦截器解析出租户 ID: `"test"`
3. 不同租户的记录互不可见
4. 可通过修改前端 `src/api/index.js` 中的 Authorization 头测试

### 测试 5: Redis 缓存（可选验证）
1. 第一次输入某症状，观察后端日志调用 AI
2. 清空 Redis 或等待缓存过期
3. 再次输入相同症状
4. 如果实现缓存逻辑，第二次应直接返回缓存结果

## 预期效果截图要点

录屏时请确保展示：
1. ✅ 完整的问诊流程（输入 → 提交 → 结果展示）
2. ✅ 风险高亮的视觉效果
3. ✅ 历史记录列表和展开详情
4. ✅ 页面切换流畅

## 常见问题排查

### 问题 1: 前端请求失败，CORS 错误
**解决**: 确保后端已配置跨域支持，检查 `SpringConfig.java`

### 问题 2: AI 返回格式错误
**解决**: 
- 检查 `application.properties` 中 AI API 配置
- 查看后端日志确认模型响应
- 验证 Prompt 是否正确

### 问题 3: 数据库连接失败
**解决**:
- 确认 MySQL 服务已启动
- 检查 `application.properties` 中的数据库用户名密码
- 确认已执行 `init.sql`

### 问题 4: Token 鉴权失败（401）
**解决**:
- 检查前端 `src/api/index.js` 中的 Authorization 头
- 确认后端拦截器配置正确
- 查看浏览器开发者工具 Network 面板的请求头

## 性能优化建议（加分项展示）

如果需要展示更多技术亮点：
1. **Loading 状态**: 提交时禁用按钮，防止重复提交
2. **错误处理**: 友好的错误提示
3. **响应式设计**: 移动端适配良好
4. **动画效果**: 结果卡片淡入、加载旋转动画

## 录屏脚本参考

```
0:00 - 0:30   项目介绍和技术栈说明
0:30 - 2:00   演示基本问诊流程（普通症状）
2:00 - 3:00   演示风险高亮功能（胸痛/呼吸困难）
3:00 - 4:00   展示历史记录列表和详情
4:00 - 5:00   Prompt 设计思路讲解（V1 vs V2 对比）
5:00 - 6:00   展示 AI 工具使用过程（Cursor/Gemini）
6:00 - 7:00   演示发现并修复的问题（JSON 格式异常）
7:00 - 8:00   代码结构讲解（前后端架构）
8:00 - 9:00   多租户鉴权和 Redis 缓存说明
9:00 - 10:00  总结和亮点回顾
```

祝测试顺利！🎉
